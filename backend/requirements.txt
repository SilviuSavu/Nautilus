# NautilusTrader with Interactive Brokers adapter
# Note: Requires Python 3.11+ (we use Python 3.13.7 via pyenv)
nautilus_trader[ib]==1.219.0

# FastAPI and web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic[email]==2.5.0
python-multipart==0.0.6
websockets==12.0
httpx==0.25.2
python-dotenv==1.0.0
pydantic-settings==2.1.0

# Database and caching
redis==5.0.1
asyncpg==0.29.0
cachetools==6.1.0
aiohttp==3.12.15

# Parquet export for NautilusTrader compatibility
pandas>=2.2.3
pyarrow>=20.0.0
scipy>=1.11.0

# Risk analysis dependencies
scikit-learn>=1.3.0

# Market data providers
ibapi==9.81.1.post1
yfinance==0.2.28

# Utilities
psutil==5.9.6

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1

# Authentication
PyJWT==2.8.0
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# Data export dependencies (Story 5.3)
openpyxl>=3.1.0
reportlab>=4.0.0

# EDGAR API connector dependencies (simplified)
httpx>=0.25.2
lxml>=4.9.0
beautifulsoup4>=4.12.0

# FRED Economic Data API dependencies  
aiohttp>=3.9.0

# Alpha Vantage API integration dependencies
# (Uses existing aiohttp and httpx for HTTP client functionality)

# Trading Economics API integration dependencies
tradingeconomics>=0.3.0

# DBnomics economic data integration
dbnomics>=1.2.4

# Toraniko Factor Engine dependencies
numpy~=1.26.2
polars~=1.0.0

# Sprint 3: Advanced Trading Infrastructure Dependencies
# WebSocket support (already included above: websockets==12.0)
python-socketio==5.8.0

# Performance monitoring
prometheus-client==0.17.1
grafana-api==1.0.3

# Advanced analytics (pandas and scipy already included above)
# pandas>=2.2.3 (already included)
# scipy>=1.11.0 (already included)

# Message queuing
celery==5.3.1

# Additional async support
asyncio-mqtt==0.16.1

# Ultra-Performance Optimization Dependencies
# GPU Acceleration (CUDA)
cupy>=12.0.0  # CUDA-accelerated NumPy for GPU computing
pynvml>=11.5.0  # NVIDIA GPU monitoring and management

# Ultra-Low Latency Optimizations
uvloop>=0.19.0  # High-performance event loop for asyncio
numba>=0.59.0  # JIT compiler for numerical functions

# Advanced Serialization and Compression
msgpack>=1.0.7  # Fast binary serialization
orjson>=3.9.0  # Ultra-fast JSON serialization
lz4>=4.3.0  # Fast compression for network/storage
flatbuffers>=23.5.0  # Zero-copy serialization

# Memory Optimization
tracemalloc  # Built-in Python memory profiler (standard library)
pymalloc  # Python memory allocator (standard library) 
line-profiler>=4.1.0  # Line-by-line performance profiling

# Network Performance
gevent>=23.9.0  # High-performance networking library
eventlet>=0.33.0  # Concurrent networking library

# Advanced Caching
redis-py-cluster>=2.1.3  # Redis clustering support
hiredis>=2.2.0  # Fast Redis protocol parser

# Machine Learning for Predictive Optimization
scikit-learn>=1.3.0  # Already included above for risk analysis
# numpy>=1.26.2 (already included as numpy~=1.26.2)

# Performance Monitoring and Profiling
py-spy>=0.3.14  # Sampling profiler for Python programs
memory-profiler>=0.61.0  # Memory usage monitoring
psutil>=5.9.6  # Already included above

# Optional Dependencies for Maximum Performance
# Uncomment these if you have the required system libraries:
# pyzmq>=25.1.0  # ZeroMQ for ultra-fast messaging
# cython>=3.0.0  # For compiling performance-critical code to C
# bottleneck>=1.3.7  # Fast NumPy array functions